package com.mygdx.game;

// Classe contenant les variables constantes qui peuvent être utilisées par tout les fichiers sans importer ladite classe
public class Global {
    // public static rend la variable globale
    public static int WIDTH = 560;
    public static int HEIGHT = 640;
    public static int BAN_HEIGHT = 80;
    public static int BOARD_HEIGHT = HEIGHT - BAN_HEIGHT;
}
